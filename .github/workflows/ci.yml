name: CI

on:
  pull_request:
    branches-ignore:
      - MSVC
  push:
    branches-ignore:
      - master
      - MSVC
    tags-ignore:
      - "v*"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    with:
      ref: ${{ github.ref }}
      windows_profile_build: false
